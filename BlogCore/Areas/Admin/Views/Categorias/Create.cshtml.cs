using Microsoft.AspNetCore.Mvc.RazorPages;

// This code-behind was scaffolded as a Razor Page PageModel, but the Admin area uses MVC controllers.
// To avoid the runtime treating this view as a Razor Page (which can cause ViewData to be null),
// we replace the PageModel with an inert placeholder class. The MVC controller will supply the model.
namespace BlogCore.Areas.Admin.Views.Categorias
{
    internal class Create_ViewPlaceholder { }
}